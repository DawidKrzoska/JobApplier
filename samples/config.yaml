job_sources:
  - type: linkedin
    options:
      keywords: "C++"
      location: "Poland"
      limit: 1000
      remote: true
      # Provide your LinkedIn li_at cookie via env var or direct string.
      session_cookie: "${LINKEDIN_LI_AT}"
notifications:
  channel: cli
storage:
  path: .jobapplier-state.json
approvals:
  min_score: 2
scoring:
  skill: 5
